pt-BR:
  activerecord:
    attributes:
      exchange_rate:
        real: Cotação
  errors:
    format: "%{attribute} %{message}"
    messages:
      outdated_exchange_rate: utilizada não está atualizada
